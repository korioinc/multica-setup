#!/usr/bin/env python3
"""Compatibility entry point for running multica-setup from a source checkout."""

import importlib
import sys
from pathlib import Path

source_root = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(source_root))

main = importlib.import_module("multica_setup.cli").main


if __name__ == "__main__":
    raise SystemExit(main())
